name: Release Artifacts

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  desktop-artifacts:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: macos-latest
            platform: macos

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version: "1.3.14"

      - name: Cache Bun install cache
        uses: actions/cache@v4.2.3
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Linux dependencies
        if: ${{ matrix.platform == 'linux' }}
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev libxdo-dev libfuse2

      - name: Setup Rust
        run: |
          rustup toolchain install stable --profile minimal
          rustup default stable
          rustup component add rustfmt clippy

      - name: Build sidecar
        run: bun run build

      - name: Copy sidecar
        run: bun run app:copy-sidecar

      - name: Build Tauri app
        env:
          APPIMAGE_EXTRACT_AND_RUN: "1"
        run: bun run tauri build

      - name: Upload Linux artifacts
        if: ${{ matrix.platform == 'linux' }}
        uses: actions/upload-artifact@v4.6.2
        with:
          name: overlearn-desktop-linux
          if-no-files-found: error
          path: |
            src-tauri/target/release/bundle/deb/*.deb

      - name: Upload macOS artifacts
        if: ${{ matrix.platform == 'macos' }}
        uses: actions/upload-artifact@v4.6.2
        with:
          name: overlearn-desktop-macos
          if-no-files-found: error
          path: |
            src-tauri/target/release/bundle/dmg/*.dmg

      - name: Attach bundles to the GitHub Release
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          # release.yml creates the release on the same tag push; tolerate the race.
          gh release create "$GITHUB_REF_NAME" --generate-notes 2>/dev/null || true
          if [ "${{ matrix.platform }}" = "linux" ]; then
            gh release upload "$GITHUB_REF_NAME" src-tauri/target/release/bundle/deb/*.deb --clobber
          else
            gh release upload "$GITHUB_REF_NAME" src-tauri/target/release/bundle/dmg/*.dmg --clobber
          fi
