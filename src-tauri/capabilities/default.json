{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window access for the Overlearn launcher.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
