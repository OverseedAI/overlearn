{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window access for the Overlearn desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
